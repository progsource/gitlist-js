extend repoNavigation
block append content
  if codemode == 'image'
    //img(src='#{fileContent}')
    p
      = 'Image: ' + fileContent
  else
    textarea(class='codeOutput', data-codemode='#{codemode}')
      = fileContent